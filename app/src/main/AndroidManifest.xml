<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unipiloto.appvacov">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".Sitio_verInforme"></activity>
        <activity android:name=".Perso_ubicx" />
        <activity android:name=".sitio_ingresarVac" />
        <activity
            android:name=".perso_ruta"
            android:label="@string/title_activity_perso_ruta" />
        <activity android:name=".perso_informe" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".distri_inventario" />
        <activity android:name=".distri_asignacion" />
        <activity android:name=".distri_estado_distribucion" />
        <activity android:name=".Distribuidor_main" />
        <activity android:name=".Sitio_asignar" />
        <activity android:name=".perso_asignados" />
        <activity android:name=".Sitio_main" />
        <activity android:name=".Perso_main" />
        <activity android:name=".Sitio_vac" />
        <activity android:name=".User_vac" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".Adapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>